task_name: fit_image_test
train: true
test: false
extras:
  ignore_warnings: true
  enforce_tags: true
  print_config: true
logger:
  wandb:
    name: init_try
    save_dir: ${paths.output_dir}
    offline: true
    project: image_fit_test
paths:
  root_dir: ${oc.env:PROJECT_ROOT}
  data_dir: ${paths.root_dir}/data/
  log_dir: ${paths.root_dir}/logs/
  output_dir: ${hydra:runtime.output_dir}
  work_dir: ${hydra:runtime.cwd}
trainer:
  trainer:
    _target_: src.models.trainer_image.ImageTrainer
    scene_path: /Users/wangzh/Desktop/ShanghaiTech/Spring_2025/ICCP2025/mitsuba3-workflow/scenes/cbox.xml
    learning_rate: 0.1
    max_stages: 1
    max_iterations: 500
    val_interval: 1
    device: cuda
